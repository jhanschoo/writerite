mutation CardDelete(
  $answers: [String!]
  $fullAnswer: JSONObject
  $id: ID!
  $mainTemplate: Boolean
  $prompt: JSONObject
  $template: Boolean
) {
  cardDelete(id: $id) {
    ...cardDetail
  }
}
