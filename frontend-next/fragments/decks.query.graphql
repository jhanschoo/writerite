query Decks(
  $cursor: ID
  $scope: DecksQueryScope
  $stoplist: [ID!]
  $take: Int
  $titleFilter: String
) {
  decks(
    cursor: $cursor
    scope: $scope
    stoplist: $stoplist
    take: $take
    titleFilter: $titleFilter
  ) {
    ...deckSummary
  }
}
