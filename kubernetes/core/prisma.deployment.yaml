apiVersion: apps/v1
kind: Deployment
metadata:
  name: prisma
spec:
  selector:
    matchLabels:
      app: prisma
      env: production
      tier: backend
  template:
    metadata:
      labels:
        app: prisma
        env: production
        tier: backend
    spec:
      containers:
      - env:
        - name: PRISMA_CONFIG
          value: |
            port: 4466
            databases:
              default:
                connector: postgres
                host: postgres
                user: prisma
                password: prisma
                rawAccess: true
                port: 5432
                migrations: true
        image: prismagraphql/prisma:1.34
        name: prisma
        ports:
        - containerPort: 4466
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
