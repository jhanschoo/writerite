schema: ./generated/schema.graphql
# documents: './test/**/*.ts'
generates:
  ./generated/gql/:
    preset: client
    plugins: []
  ./generated/schema.graphql:
    plugins: [schema-ast]
  ./generated/typescript-operations.ts:
    plugins:
      - typescript
      - typescript-operations
      - add:
          content: "import { JSONValue, JSONObject } from '../src/types/jsonTypes'"
config:
  scalars:
    DateTime: string
    EmailAddress: string
    JSON: JSONValue
    JSONObject: JSONObject
    JWT: string
    UUID: string
