apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "writerite-backend-apollo.fullname" . }}-test-connection"
  labels:
{{ include "writerite-backend-apollo.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "writerite-backend-apollo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
